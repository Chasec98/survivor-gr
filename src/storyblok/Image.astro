---
import { storyblokEditable } from '@storyblok/astro';

interface Props {
  blok: {
    image: {
      filename: string;
      alt: string;
    };
  };
}

const { blok } = Astro.props as Props;
---
<img src={blok.image.filename} alt={blok.image.alt} {...storyblokEditable(blok)} />
